# 发布指南 - 生成全平台可执行文件

本指南说明如何发布新版本并自动生成 Linux、Windows、macOS 三个平台的可执行文件。

## 📋 前置要求

1. **Git 配置完成**
   - SSH 密钥已添加到 GitHub
   - 本地仓库已关联远程仓库

2. **GitHub CLI 已认证**（可选，用于创建 Release）
   ```bash
   gh auth login
   ```

## 🚀 发布新版本的完整流程

### 步骤 1: 完成代码修改

确保所有代码修改已完成并测试通过。

### 步骤 2: 提交代码

```bash
# 进入项目目录
cd /home/wu/桌面/pdf_zhongjiban

# 查看修改状态
git status

# 添加所有修改的文件
git add .

# 提交修改（使用规范的提交信息）
git commit -m "feat: 描述你的新功能

- 详细说明1
- 详细说明2
- 详细说明3

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

**提交信息规范**：
- `feat:` - 新功能
- `fix:` - 修复bug
- `docs:` - 文档更新
- `refactor:` - 代码重构
- `ci:` - CI/CD配置更新

### 步骤 3: 创建版本标签

```bash
# 创建标签（注意：使用大写V，如V3.0、V3.1等）
git tag -a V3.0 -m "V3.0 版本标题

新增功能：
- 功能1
- 功能2
- 功能3"

# 查看标签是否创建成功
git tag -l
```

**版本号规范**：
- 主版本号：重大功能变更（V1.0 -> V2.0）
- 次版本号：新增功能（V2.0 -> V2.1）
- 修订号：bug修复（V2.1.0 -> V2.1.1）

### 步骤 4: 推送到 GitHub

```bash
# 推送代码到 master 分支
git push origin master

# 推送标签（这会触发 GitHub Actions 自动构建）
git push origin V3.0
```

**重要**：推送标签后，GitHub Actions 会自动开始构建三个平台的可执行文件。

### 步骤 5: 监控构建进度

访问 GitHub Actions 页面查看构建状态：
```
https://github.com/stflj2022/-PDF-/actions
```

构建过程大约需要 **10-15 分钟**，包括：
- ✅ Linux x86_64 可执行文件
- ✅ Windows x86_64 可执行文件
- ✅ macOS x86_64 可执行文件

### 步骤 6: 创建 GitHub Release（可选）

如果 GitHub Actions 没有自动创建 Release，可以手动创建：

**方法 A：使用 GitHub CLI（推荐）**

```bash
gh release create V3.0 \
  --title "V3.0 版本标题" \
  --notes "## 🎉 V3.0 版本标题

### ✨ 新增功能
- 功能1
- 功能2

### 🔧 技术改进
- 改进1
- 改进2

### 📝 使用说明
1. 下载对应平台的文件
2. 解压并运行

---
Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

**方法 B：在 GitHub 网页上创建**

1. 访问：https://github.com/stflj2022/-PDF-/releases
2. 点击 "Draft a new release"
3. 选择标签：V3.0
4. 填写标题和说明
5. 点击 "Publish release"

## 📦 构建产物说明

GitHub Actions 会自动生成以下文件并上传到 Release：

| 平台 | 文件名 | 说明 |
|------|--------|------|
| Linux | `pdf-retypeset-V3.0-linux-x86_64.tar.gz` | Linux 64位可执行文件 |
| Windows | `pdf-retypeset-V3.0-windows-x86_64.zip` | Windows 64位可执行文件 |
| macOS | `pdf-retypeset-V3.0-macos-x86_64.tar.gz` | macOS 64位可执行文件 |

## 🔧 本地构建（可选）

如果需要在本地构建可执行文件：

### Linux/macOS

```bash
# 安装依赖
pip install pyinstaller

# 构建
pyinstaller --clean --noconfirm pdf-retypeset.spec

# 可执行文件位于 dist/ 目录
ls -lh dist/
```

### Windows

```powershell
# 安装依赖
pip install pyinstaller

# 构建
pyinstaller --clean --noconfirm pdf-retypeset.spec

# 可执行文件位于 dist\ 目录
dir dist\
```

## ❌ 常见问题

### 1. 推送标签失败

**问题**：`Permission denied (publickey)`

**解决**：
```bash
# 检查 SSH 密钥
ssh -T git@github.com

# 如果失败，重新配置 SSH 密钥
ssh-keygen -t ed25519 -C "your_email@example.com"
# 将 ~/.ssh/id_ed25519.pub 添加到 GitHub
```

### 2. GitHub Actions 构建失败

**问题**：构建过程中出错

**解决**：
1. 访问 Actions 页面查看详细错误日志
2. 检查 `requirements.txt` 是否包含所有依赖
3. 检查 `.github/workflows/build-release.yml` 配置

### 3. 标签已存在

**问题**：`tag 'V3.0' already exists`

**解决**：
```bash
# 删除本地标签
git tag -d V3.0

# 删除远程标签
git push origin :refs/tags/V3.0

# 重新创建标签
git tag -a V3.0 -m "版本说明"
git push origin V3.0
```

### 4. gh CLI 认证失败

**问题**：`no oauth token found`

**解决**：
```bash
# 重新登录
gh auth login

# 选择：
# 1. GitHub.com
# 2. HTTPS 协议
# 3. Login with a web browser
# 4. 在浏览器中完成授权
```

## 📝 发布检查清单

发布前请确认：

- [ ] 所有代码已提交
- [ ] 版本号已更新（如果有版本文件）
- [ ] README.md 已更新
- [ ] 测试通过
- [ ] 提交信息清晰明确
- [ ] 标签信息详细完整
- [ ] GitHub Actions 配置正确

## 🔗 相关链接

- GitHub 仓库：https://github.com/stflj2022/-PDF-
- Actions 页面：https://github.com/stflj2022/-PDF-/actions
- Releases 页面：https://github.com/stflj2022/-PDF-/releases

## 📞 技术支持

如有问题，请在 GitHub Issues 中提出：
https://github.com/stflj2022/-PDF-/issues

---

**最后更新**：2026-02-08
**适用版本**：V3.0+
